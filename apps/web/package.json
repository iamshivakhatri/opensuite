{
  "name": "@opensuite/web",
  "version": "0.0.0",
  "private": true,
  "description": "OpenSuite web application. Not yet implemented — see opensuite_v4_modern_minimal.html for the design reference.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo \"(no lint configured yet)\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
