# Launch media

Add only real captures from OpenSuite here.

The highest-value first asset is a 20–40 second recording: open an existing DOCX, ask for one targeted change, show the resulting version, then download it. Do not use a mockup or synthetic product screenshot.

Suggested names:

- `docx-agent-edit.gif` for a short README demo
- `docx-agent-edit.mp4` for a higher-quality video
- `workspace-docx.png` for a real product screenshot
